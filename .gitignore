/node_modules/
/plans/
/docs/
/dist/
/.superpowers/
.DS_Store
.idea/
.vscode/
*.log
coverage/
*.tsbuildinfo

# Vercel CLI project link (points at a personal deployment, not the client's).
.vercel

# Deployment-specific configuration (backend URL, etc.) — every developer/
# deployment sets their own; .env.example documents the shape.
.env
.env.*
!.env.example
